[workspace]
resolver = "2"
members = [
    "crates/fastled-cli",
]

[workspace.package]
version = "2.0.20"
edition = "2021"
rust-version = "1.95"
license = "MIT"
repository = "https://github.com/zackees/fastled-wasm"
homepage = "https://github.com/zackees/fastled-wasm"

[workspace.dependencies]
kernal-api = { version = "=0.1.0", features = ["fs", "fs-watch", "hash-sha256", "archive", "http-client", "http-server", "websocket", "event-stream", "secure-random", "text-similarity", "pty", "terminal-input", "terminal-style", "command-arguments", "command-schema", "config-toml", "source-cpp", "json", "error-context"] }

[profile.release]
debug = "line-tables-only"
split-debuginfo = "packed"
strip = "none"

# Temporary migration override; replace with the verified published release.
# A git revision keeps CI hermetic while the crates.io release is pending.
[patch.crates-io]
kernal-api = { git = "https://github.com/zackees/kernal-api.git", rev = "264308e4db3b6da79cdb7cd66f7a4816c6f2cb49" }

[profile.dev.package.kernal-api]
codegen-units = 1

[profile.test.package.kernal-api]
codegen-units = 1
